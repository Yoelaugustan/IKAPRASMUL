# Backend API URL — used server-side by Next.js route handlers and RSC fetches.
# In development: http://localhost:5080
# In production: internal URL if BE and FE share a server, or the public HTTPS URL otherwise.
API_URL=http://localhost:5080
